# Core — pin transformers: 5.x uses a threaded checkpoint loader that segfaults on
# Windows for InstructBLIP / LLaVA-1.6 / Qwen-VL.
transformers==4.49.0
torch>=2.1
accelerate>=0.26
huggingface_hub>=0.23
safetensors>=0.4
sentencepiece>=0.1.99
protobuf>=3.20

# Qwen-VL-Chat (trust_remote_code) extras
transformers_stream_generator>=0.0.4
tiktoken>=0.5
einops>=0.7

# Data / numerics / metrics
numpy>=1.24
scikit-learn>=1.3
Pillow>=10.0
pyarrow>=14.0
tqdm>=4.66
pycocoevalcap>=1.2          # BLEU / ROUGE / CIDEr for caption quality

# Figures + manuscript build
matplotlib>=3.8
python-docx>=1.1
